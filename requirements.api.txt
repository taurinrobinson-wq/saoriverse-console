# Minimal requirements for the API container
# Kept intentionally small so the API image stays lightweight
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.12.5
requests==2.32.3
python-multipart==0.0.6
supabase==2.6.0
jinja2==3.1.2
