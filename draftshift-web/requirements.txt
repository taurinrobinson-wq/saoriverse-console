# DraftShift Web API - Minimal Dependencies
# Note: Core packages (fastapi, uvicorn, pydantic, etc.) are pre-installed via replit.nix
# Only add optional runtime dependencies here if needed

# Utilities (if needed at runtime)
python-dotenv==1.0.1

pytest-cov==7.0.0
