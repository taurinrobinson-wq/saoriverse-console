user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 20M;

    # ===== VELINOR SERVICE =====
    # HTTP to HTTPS redirect for Velinor
    server {
        listen 80;
        listen [::]:80;
        server_name velinor.firstperson.chat;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$server_name$request_uri;
        }
    }

    # HTTPS server for Velinor
    server {
        listen 443 ssl http2;
        listen [::]:443 ssl http2;
        server_name velinor.firstperson.chat;

        # SSL certificates
        ssl_certificate /etc/letsencrypt/live/velinor.firstperson.chat/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/velinor.firstperson.chat/privkey.pem;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;

        # Proxy to Velinor container
        location / {
            proxy_pass http://velinor:8000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        location /health {
            proxy_pass http://velinor:8000/health;
            access_log off;
        }
    }

    # ===== FIRSTPERSON SERVICE =====
    # HTTP to HTTPS redirect for FirstPerson
    server {
        listen 80;
        listen [::]:80;
        server_name firstperson.chat www.firstperson.chat;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$server_name$request_uri;
        }
    }

    # HTTPS server for FirstPerson
    server {
        listen 443 ssl http2;
        listen [::]:443 ssl http2;
        server_name firstperson.chat www.firstperson.chat;

        # SSL certificates
        ssl_certificate /etc/letsencrypt/live/firstperson.chat/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/firstperson.chat/privkey.pem;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;

        # Backend API routes
        location /api/ {
            proxy_pass http://firstperson:8000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        # Direct backend endpoints
        location /health {
            proxy_pass http://firstperson:8000/health;
            access_log off;
        }

        location /chat {
            proxy_pass http://firstperson:8000;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        # Frontend (everything else)
        location / {
            proxy_pass http://firstperson:3001;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }
    }
}
