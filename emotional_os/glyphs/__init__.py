
"""Package initialization for `emotional_os.glyphs`.

This package exposes glyph-related utilities and submodules. The
original file accidentally contained markdown-style fences which made
it invalid Python during imports. Keep this file minimal so Python
recognizes the package and submodules (e.g. `tone`).
"""

__all__ = [
    # submodules are importable as `emotional_os.glyphs.<name>` once
    # the corresponding .py files exist in this package directory.
]
