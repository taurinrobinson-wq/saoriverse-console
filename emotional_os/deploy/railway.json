{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python -m uvicorn fastapi_app:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 30",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}